import { scale } from "react-native-size-matters";

const Sizes = {
	header: scale(28),
	subHeader: scale(22),
	body: scale(16),
	small: scale(11),
	label: scale(12),
	button: scale(14),
	caption: scale(14), // foto altı açıklamalar
};

export { Sizes };
